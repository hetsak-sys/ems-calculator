# Hetsa PowerSuite — Debt Register
## (`/docs/debt.md` per HAIOS Appendix B — [DBT-2])

**Split out from `Hetsa_PowerSuite_Project_Knowledge.md` on 2026-07-25**, closing the debt item that
tracked this document not existing as its own file. Content is unchanged from the source doc at split
time — this is an extraction, not a rewrite. Each entry: what the shortcut is, why it was taken, its
repayment trigger, and severity, per [DBT-2].

---

## Debt Register

## 8. Debt Register (condensed) [DBT-2]

A full `/docs/debt.md` doesn't exist yet as a separate file — logged here until it's worth splitting out.

| Debt | Why taken | Repayment trigger | Severity |
|---|---|---|---|
| ~~On-device verification of 2026-07-25 test-suite/refactor sweep~~ | Automated suite (315 tests) proved formula correctness; UI wiring (real rendering, rounding/formatting, touch input) still needed physical confirmation | **Repaid (2026-07-25)** — every module in `docs/on_device_checklist.md` (Motor ×6, Earthing ×4, Cable ×8, Power Systems ×4, ContactorOLR, Generator Sizing ×2 scenarios, Power Quality ×3) matched expected values exactly on the reference device; both app-wide checks (lazy-load flash on tab switch, kill-mid-calculation recovery) also passed | Closed |
| ~~[COD-14] gap found in Earthing, Cable, and PowerSys during extraction: none of their calculators cleared the previous result before validating new input~~ | Same bug class already fixed in Protection/Motor/ContactorOLR, but these three modules were missed in that earlier pass | **Repaid (2026-07-25)** — `setRes(null)`/`setResult(null)`/`setResults(null)` added at the top of all affected calculate() functions across all three files (4 in Earthing, 8 in Cable, 4 in PowerSys), before validation, matching the established [COD-14] pattern | Closed |
| ~~No automated test suite for most modules~~ | Solo-dev velocity; manual verification so far | **Repaid (2026-07-25)** — every calculation module now has an extracted engine + test suite: `protectionCoordinationEngine.js` (40), `earthFaultProtectionEngine.js`/`relaySelectionEngine.js` (32), `motorEngine.js` (39), `earthingEngine.js` (17), `cableEngine.js` (37), `powerSysEngine.js` (16), `generatorDerating.js`/`gridTieCompliance.js`/`hybridSizing.js` (35, closing the gap in Renewable Energy's dependency libs), `calcEngine.js` (49, QuickMath's parser/evaluator), `contactorOlrEngine.js` (15), `generatorSizingEngine.js` (18), `pqEngine.js` (15, Power Quality — found and closed during the final sweep, wasn't in the original 8-module scope) — **315 tests passing repo-wide, full build clean**. This item is closed; ongoing discipline is to extend the pattern to any *new* calculator module from day one, not to backfill again | Closed |
| **`GeneratorSizing.jsx`'s local `pf()` helper never had comma-decimal normalization** — every other module's `pf()` does `parseFloat(String(v).replace(',', '.'))`, but this file's local copy was plain `parseFloat(v)`, silently truncating a comma-decimal entry (e.g. "15,5" → 15) instead of normalizing it. Exactly the recurring risk class already flagged once (2026-07-22, 18 fields in this same file) | Found 2026-07-25 during the `generatorSizingEngine.js` extraction — the engine's `pf()` was written to match the project-wide convention, which is a real behavior change from the file's own prior local helper, not a neutral refactor | **Repaid (2026-07-25)** — `GeneratorSizing.jsx` now imports `pf()` from `generatorSizingEngine.js`, which normalizes commas like every other module. Flagged explicitly rather than silently bundled in, per [AI-8] | Closed — but flagged for on-device re-verification: if any saved/default value in this module's fields relied on the old truncating behavior, confirm it still parses as expected |
| **Dead code: `ContactorCalc`/`OverloadCalc` in `MotorCalculator.jsx`** (defined, never rendered) | Found 2026-07-25 during Motor extraction | Cheap to remove whenever Motor is next touched | Cosmetic — genuinely unreachable, no functional risk, but violates [COD-5] |
| **Correction (2026-07-25): the previous entry here was factually wrong.** It claimed `CableSizing` was "the one sub-tab without PDF export, unlike its siblings." Rechecked: **none** of Cable's 8 sub-tabs (CableSizing, VoltDrop, ShortCircuit, TrailingCable, ConduitFill, GlandSize, CableSchedule, VfdCable) have PDF export wired up anywhere — no `ResultCard`/`showCard`/`pdfExport.js` usage exists in the file at all, except the one dead `useResultCard()` call in `CableSizing` that started this investigation. So this isn't "one tab missing a button other tabs have" — it's the entire Cable module never having had PDF export built, unlike Motor/Protection/PowerSys/Renewable/PQ, which all do | Found 2026-07-25 during the previous session's confirmation sweep; the "unlike its siblings" framing wasn't checked against the other 7 sub-tabs before being written down | **Not fixed** — this is a real feature-scope decision (wiring PDF export into 8 calculators, following the `ResultCard`/`useResultCard`/`pdfExport.js` pattern already established elsewhere), not a one-line cleanup. Needs Hertz's go-ahead before building, same as any other module-level feature addition | Cosmetic-to-should — no functional risk today, but a real, larger product gap than previously described. The dead `useResultCard()` call in `CableSizing` should be removed regardless of the export decision, since it does nothing either way |
| **Dead code found during Motor extraction: `ContactorCalc`/`OverloadCalc` in `MotorCalculator.jsx` (lines ~293–371) are defined but never rendered** — `MOTOR_TABS`'s `'qf'` entry uses the imported `ContactorOLR.jsx` component instead | Discovered 2026-07-25 while extracting `motorEngine.js`; not fixed in the same pass since it's out of scope for a behavior-neutral test-extraction refactor ([REF-3]) | Cheap to remove whenever Motor is next touched — delete both functions, confirm nothing else references them, one-line change | Cosmetic — genuinely unreachable code, no functional risk, but violates [COD-5] ("no dead code in committed work") |
| No CI test/lint gate on `main` (a `deploy.yml` workflow exists but only builds+deploys to GitHub Pages, doesn't run tests) | No team-coordination problem yet; deploy workflow predates the test suite | Before public distribution, or before a second developer joins — add an `npm test` step to `deploy.yml` as the cheap first fix | Cosmetic-to-risky. Confirmed 2026-07-24: still just tracked, description corrected from "no CI pipeline exists" (see §2) |
| **Duct Derating reuses the direct-buried grouping table (`GROUPING_DIRECT_BURIED`) rather than a duct-bank-specific one** | No IEC 60364-5-52/60502-2 duct-bank grouping table (distinct from the direct-buried Table B.52.18 used here) could be verified with confidence during the 2026-07-26 Underground Reticulation build — using an unverified number would have been worse than reusing a verified-but-approximate one. Flagged in the `ductDerating()` return value (`factors.groupingReused: true`) and in the tab's info box, not silently applied | Revisit if a verified duct-bank-specific grouping table is found, or before any large duct-bank installation (>4 circuits) is sized for something safety-critical — confirm against a full thermal study in the meantime | Should — flagged and low-risk for typical field-quick sizing (the direct-buried table is a reasonable conservative proxy), but not a substitute for the correct duct-specific figures if a large critical installation ever needs one |
| **Direct-Buried Sizing's grouping table (`GROUPING_DIRECT_BURIED`) has one interpolated cell** — the 16-circuits/0.5m-clearance source transcription (0.38) broke the monotonic trend every other row in the table follows (each row should rise with increasing clearance); replaced with 0.66, interpolated from the 12- and 20-circuit rows' own 0.5m values (0.71, 0.66) | Found 2026-07-26 while sourcing IEC 60364-5-52 Table B.52.18 for the Underground Reticulation build — treated as a transcription artifact in the secondary source rather than a real standard value, flagged in a code comment rather than silently "corrected" and hidden | Revisit if the primary IEC 60364-5-52 standard text itself can be checked directly (the AI's search access only reached secondary-source reproductions of the table) | Cosmetic — affects only the specific 16-circuit/0.5m-clearance combination, a narrow and unlikely field case; flagged for traceability rather than because it's currently causing wrong results |
| ~~Underground Reticulation (§5.6.2) built but not yet run on-device~~ | New engine functions (`directBuriedSizing`, `ductDerating`, `routeFaultLevel`) proved correct against 16 hand-calculated automated tests (331 passing repo-wide); UI wiring still needed physical confirmation before being trusted | **Repaid (2026-07-26)** — all scenarios in `docs/on_device_checklist_underground_reticulation.md` (Direct-Buried ×2, Duct Derating ×2, Route Fault Level ×1 three-segment route) matched expected values exactly on the reference device; both app-wide UI checks (conditional clearance selector, resistivity-mode input swap) also passed. **Confirmed committed and pushed to `origin/main` (2026-07-26)** — commit `dae503a`, independently re-verified via fresh clone: 331 tests passing, clean build | Closed |
| ~~Installation Design (§5.6.1) — Area Lighting sub-tab not yet built~~ | Last of the four planned sub-tabs (Load Assessment, DB Sizing, Circuit Design shipped 2026-07-26) | **Repaid (2026-07-27)** — built (`InstallationDesign.jsx`/`installationDesignEngine.js`), 43/43 tests passing in `installationDesignEngine.test.js`, on-device verified against `docs/on_device_checklist_installation_design.md`'s Area Lighting scenario (40m×80m/20lux/CU 0.4/MF 0.8/20000lm-200W → 10 fittings, 20 lux actual, 20.0m mounting height, 80.0m pole spacing, 2000W, 0.63 W/m², matched exactly). **§5.6.1 is now fully closed.** Confirmed on `origin/main` as commit `210ed02`, independently re-verified via fresh clone: 380 tests passing repo-wide, clean build | Closed |
| **`pqEngine.js`'s harmonics/UPS/lighting inputs never normalized comma-decimals** — the same recurring risk class flagged 2026-07-22 (18 fields in `GeneratorSizing.jsx`) and 2026-07-25 (the module's own `pf()` helper), this time found in Power Quality's engine | Found and fixed 2026-07-27 during the Area Lighting build, while extracting the shared lumen-method core out of `pqEngine.js` — the comma-decimal gap surfaced as a side effect of touching that code, not from a dedicated audit | **Repaid (2026-07-27)**, same commit (`210ed02`) as the Area Lighting ship | Closed — see the dedicated repo-wide sweep entry below, done the same day |
| **Repo-wide comma-decimal sweep** (standing action item from the three entries above, finally done deliberately rather than found a fourth time) | A grep across every file using `parseFloat`/`parseInt` (18 files) turned up **five more real gaps**, not caught by any existing test: (1) `earthingEngine.js` — all 4 functions had **zero** comma handling at all, not even a broken local helper; safety-relevant (IEEE 80 touch/step voltage, fault-loop pass/fail); (2) `powerSysEngine.js` — same, all 4 functions (transformer, PF correction, busbar, motor starting); (3) `ConvertCalculator.jsx`'s `convert()` — raw `parseFloat(input)` on the Unit Converter's core text field; (4) `RenewableEnergyCalculator.jsx`'s local `pf()` — **the most dangerous of the five**, since it reused the exact name of the project's standard comma-safe helper but was actually `parseFloat(v) \|\| fallback` with no comma stripping, silently broken across all 35 call sites in the PV/battery/grid-tie/hybrid module while looking identical to the safe pattern everywhere else; (5) `QuickMath.jsx`'s pass/fail workspace threshold field | **Repaid (2026-07-27).** `earthingEngine.js`/`powerSysEngine.js` got local comma-tolerant helpers (named `cnum`/`cint` in `powerSysEngine.js` specifically, to avoid colliding with `pf` already used there as a power-factor parameter name) plus 8 new regression tests proving comma- and period-decimal inputs now produce identical output (388/388 tests passing repo-wide, up from 380). `ConvertCalculator.jsx`, `RenewableEnergyCalculator.jsx`, and `QuickMath.jsx` fixed inline, then **on-device confirmed (2026-07-27)** — comma-decimal entry spot-checked in Earthing, Power Systems, Renewable Energy, Unit Converter, and Quick Math's pass/fail threshold, all returning expected (non-truncated) results after `assembleRelease` build and install. Confirmed compiled into the actual built bundles via `npm run build` + grep, not just source | **Closed — code and on-device, both confirmed** |
| **Lumen-method calculation existed twice** — Power Quality's interior Lighting tab had its own inline lux/area/CU/MF/lumens formula; Area Lighting needed the identical math for exterior use | Found while scoping Area Lighting 2026-07-26/27 — a second inline copy would have violated the project's own "any calc appearing in more than one place gets extracted to `src/lib` immediately" convention | **Repaid (2026-07-27)** — extracted to `src/lib/lumenMethod.js` with its own test file (`lumenMethod.test.mjs`); both `pqEngine.js` and `installationDesignEngine.js` now call the shared function. Commit `210ed02` | Closed |
| **`pdfExport.js` had two independent text-loss bugs, both found from an actual exported Area Lighting PDF** — (1) `sanitizeForPdf()` only handled 5 symbols (Ω, Φ, φ, →, ←); any other WinAnsi-unsafe symbol (≈ was the one that surfaced) renders as garbage AND corrupts `splitTextToSize`'s width math for the rest of that line, silently dropping text after it — this is how "This project does not have direct access to SANS 10389-1's actual exterior-lighting content" got truncated to "This project does actual exterior-lighting content"; (2) the "Standard:" badge was the one text block still using a single unwrapped `doc.text()` call — title and notes already got the wrap fix after an earlier combined-report title clipping bug, this one was missed, which is why "Standard: ...pole spacing" ran off the page edge | Found 2026-07-27 from Hertz's own exported Area Lighting PDF, not a code audit — the visible corruption (a stray character where ≈ should be, and a sentence cut off mid-word) was the actual symptom reported | **Repaid (2026-07-27).** `sanitizeForPdf` expanded to cover ≈, ≤, ≥, √, ÷, −, ∆, ≠, π, ρ, θ, η, μ, α, Σ — confirmed via a repo-wide scan that each genuinely appears in PDF-bound note/standard/label text, not just decorative UI. Standard badge now wraps via the same `splitTextToSize` pattern as title/notes. New `pdfExport.test.mjs` (first test file this module has ever had) — 7 tests, including the exact ≈ string from the failing PDF and a reproduction of the long Area Lighting standard string proving it now needs multiple lines. 395/395 tests passing repo-wide (up from 388). This affects every module's PDF export, not just Area Lighting — Cable, Renewable Energy, Generator Sizing, and Earthing all use ≈ in PDF-bound notes and would have hit the same corruption | Closed for code; **on-device re-export of the Area Lighting PDF (and ideally one of Cable/Renewable Energy/Generator Sizing/Earthing) still owed** to confirm on the actual device, not just in a Node test |
| **Three modules had incomplete or missing PDF export**, found from Hertz reporting Earthing had none at all | `EarthingCalculator.jsx` — zero export wiring across all 4 sub-tabs (Electrode Resistance, Touch/Step Voltage, Conductor Sizing, Fault Loop Impedance). `CableCalculator.jsx` — zero export wiring across all 10 calculating sub-tabs (Sizing, Volt Drop, Short Circuit, Trailing Cable, Conduit Fill, Gland Size, Direct-Buried, Duct Derating, Route Fault Level, VFD Cable). `PowerSysCalculator.jsx` — Transformer and PF Correction tabs turned out to also be missing export, not just Busbar/Motor Starting as first reported — this Transformer tab has no vector-group field and is a genuinely different calculation from the one already covered by GeneratorSizing.jsx's combined report, not a duplicate | **Repaid (2026-07-27).** All 18 sub-tabs across the 3 files now use the same `ResultCard`/`useResultCard` pattern as every other module. `CableSchedule` (Cable) intentionally left out — it's a list-builder with its own working CSV export, not a single-calculation result. 395/395 tests passing (unchanged — none of these are extracted engines with dedicated test files, consistent with the project's existing UI-vs-engine test split). Confirmed every new calculator name string compiled into the actual built bundles, not just source | **Fully closed (2026-07-27).** Hertz confirmed on-device that PDF export now works correctly across all 18 new sub-tabs in Earthing, Cable, and Power Systems. |
| No split `architecture.md`/`debt.md`/ADR index | This document currently absorbs those roles | Before the domain expansion (§5) begins in earnest — new architecture decisions (11 kV generation, reticulation) deserve real ADRs, not a table row | Risky, rising |
| ~~Six Protection sub-tabs shipped but never run on-device~~ | — | **Repaid (2026-07-22)** — all six verified on-device against hand-calculated expected values | Closed |
| ~~Stale calculation result not cleared on validation failure~~ | Pre-existing since each module was first built; found via on-device Insulation PI/DAR testing | **Repaid (2026-07-22)** — `setResult(null)` added immediately after `setError('')` at the top of all 14 affected `calculate()` functions, before any validation runs. Re-verified on-device | Closed |
| ~~Round-to-standard-then-reference ordering pattern unchecked elsewhere~~ | The zBase bug (fixed 2026-07-22) was a specific instance of a general pattern risk: any calc that rounds to a standard size and then references that standard size needs the rounding step first | **Repaid (2026-07-24)** — grepped every `nextStd(`-style call site across the codebase (`GeneratorSizing.jsx` ×3, `RenewableEnergyCalculator.jsx` ×1) plus broader `.find()`-based standard-size lookups in `CableCalculator.jsx`; traced each downstream usage. All correctly reference the rounded/standard value, not the pre-rounded input — the `zBase`/`stdKVA` fix holds and no sibling instance was found | Closed |
| ~~PDF export: Ω/φ/Φ/arrow characters render as garbage glyphs sitewide~~ | jsPDF's built-in Helvetica font is WinAnsi/Latin-1 only | **Repaid (2026-07-22)** — `sanitizeForPdf()` added to shared rendering path; header title wrapped via `splitTextToSize`; verified via rendered-and-extracted test PDFs | Closed |
| ~~PDF export: section title doesn't repeat across a page break~~ | Same root-cause investigation as the glyph fix | **Repaid (2026-07-22)** — `drawTable()` repeats the section title marked "(cont.)" on page breaks; verified with a forced 70-row test table | Closed |
| ~~[COD-14] HAIOS amendment drafted but not yet written into the handbook~~ | Amendment was scoped verbally in-session; committing it needed a deliberate v2.2 changelog entry per [PRO-7]/[PRO-8], not a casual edit | **Repaid (2026-07-23)** — handbook amended to v2.2: version header bumped, [COD-14] rule added to §10 (State hygiene), changelog entry added. Verified on the actual file via `Select-String` matching all three edits. This project's Project-knowledge attachment updated to match | Closed |
| ~~`npm audit` flagged 6 vulnerabilities (1 critical), never reviewed~~ | Surfaced in build output per [MOB-7] post-build check, previously never reviewed | **Partially repaid (2026-07-23)** — `brace-expansion` and `dompurify` resolved via `npm audit fix` (non-breaking); `esbuild`/`vite` and `tar`/`@capacitor/cli` remain, deliberately deferred — see next two entries | Downgraded from risky to tracked-and-deferred |
| **`esbuild ≤0.24.2` moderate vuln, fix requires `vite@8.1.5` (breaking, two majors ahead of validated 5.4.21)** | Vulnerability (GHSA-67mh-4wv8-2f99) only allows a malicious website to read responses from a *running local dev server* — not reachable in the built/shipped APK. Not worth a Vite major-version migration on its own | Bundle with any deliberate future Vite upgrade (e.g. if a needed feature/plugin requires it); revisit if a higher-severity esbuild/vite advisory appears | Low — dev-machine-only exposure while `npm run dev` is actively running. Confirmed 2026-07-23: still just tracked |
| **`tar ≤7.5.18` critical vuln (path traversal / arbitrary file write), fix requires `@capacitor/cli@8.4.2` (breaking, ahead of pinned Capacitor 6 stack)** | Multiple advisories (GHSA-34x7-hfp2-rc4v and others) around hardlink/symlink path traversal during tar extraction. Only exercised when `@capacitor/cli` extracts an archive (installing/updating platforms or plugins) on the dev machine — never reaches the shipped runtime/APK. A blind `--force` bump risks breaking `cap sync`/`cap copy` against the Capacitor 6-pinned project per [MOB-7] | Bundle with a deliberate, planned Capacitor version upgrade (own ADR-level decision per [ARC-1]), not a standalone `--force` run. Re-assess urgency if `@capacitor/cli` is ever run against an untrusted/third-party plugin archive, which raises real exploitability | Real but currently low-likelihood — critical CVSS severity, but the vulnerable code path isn't exercised in this project's actual workflow today. Confirmed 2026-07-23: still just tracked |
| **Main JS bundle chunk is 844.86 kB (261.39 kB gzip)** — Vite's own build-output size warning, first noticed 2026-07-23 during the post-audit-fix build check | Never previously looked at; app has grown across many modules without code-splitting being revisited | Worth investigating before/alongside any further module additions (§5) — check impact on cold-start time against the [PERF-1] ≤3s budget on the actual reference device, not just build-log size. Candidate fix: dynamic `import()` per module tab, or `manualChunks` for heavy libs (jsPDF, html2canvas) | Cosmetic-to-risky — no confirmed on-device performance problem yet, but the [PERF-1] budget is stated per HAIOS and this hasn't been measured against it. Confirmed 2026-07-23: still just tracked. The bundle grew again with the Earth Fault/Relay Selection sub-tabs (2026-07-24) but no new warning threshold was crossed |
| ~~Repo-copy of this knowledge doc was stale relative to in-session decisions~~ — the doc committed in `772322a` didn't reflect the six-sub-tab verification, stale-result fix, combined report, or PDF-encoding closures; those existed only in chat until this update | Doc updates were drafted in conversation but the corresponding `git add`/`commit` for the doc file itself was never executed alongside the code commits | **Repaid (2026-07-23)** — confirmed via `git log`/`git push` output that commit `e74481e` landed on `origin/main` | Closed |
| **HAIOS master-copy vs. per-project Claude Project attachments can drift** — the handbook lives as a single master file (`D:\Projects\Master Project Knowledge for all Projects\HETSA_AI_Operating_System_v2.md`) shared across projects, but each project's Claude Project attaches its own copy of it. Amending the master (as done this session, v2.1→v2.2) doesn't automatically propagate to other projects' attachments | Structural consequence of the master-copy-plus-per-project-attachment setup; no automatic sync mechanism exists between them | Whenever the master handbook is amended, manually re-sync every other project's Claude Project attachment in the same session — don't let it wait. **This project's own attachment is synced; other projects are not yet confirmed synced** | Risky if left unchecked across many projects — a project silently running on a stale handbook version could miss a binding rule ([COD-14] itself is a good example of the kind of rule this risk applies to) |

---

## Entries added 2026-07-28 (Overhead Reticulation continuation session, part 2 — HV/EHV clearances)

| Debt | Why taken | Repayment trigger | Severity |
|---|---|---|---|
| ~~**HV (132kV) / EHV (220kV+) clearance figures are NOT verified**~~ | **RESOLVED 2026-07-28** — Eskom ESKASABG3 Rev 1 (May 2000), "Standard for Bush Clearance and Maintenance Within Overhead Powerline Servitudes", Annex C (normative) was identified as the missing source. Its safety clearance column explicitly cites "(OHSA, No. 85 of 1993)" as its source, and three overlap values (11/22/88kV → 0.20/0.32/1.00 m) match the independently verified Reg 15 data exactly, providing strong cross-validation. Minimum safety clearances and servitude widths are now verified in the engine for 132/275/400/765kV (and 533kV DC). Ground/road/building clearances remain unverified for HV/EHV (ESKASABG3 Annex C does not contain them) and are still honestly flagged — consult IEC 61936-1 directly. 220kV remains unverified (not a standard Eskom AC network voltage). | Closed for safety clearances. Ground/road/building clearances for HV/EHV remain open — no verified source found. | Was Should, now Cosmetic for the remaining gap |
| Voltage-class (LV/MV/HV/EHV) convention is a general engineering convention, not itself a regulatory citation | Added to categorize/label voltages for the user, distinct from the Reg 15 clearance-band boundaries which don't align to LV/MV/HV/EHV edges | None — this is intentional and disclaimed in-app (`VOLTAGE_CLASS_CONVENTION` string explicitly says "not an OHS Act clearance boundary") | Cosmetic |

## Entries added 2026-07-28 (Overhead Reticulation continuation session)

| Debt | Why taken | Repayment trigger | Severity |
|---|---|---|---|
| **Overhead Reticulation — all seven sub-tabs owe on-device verification** (Conductor Sizing temp/rate selectors + Rate A/B PDF rows, Rabbit's amber dimensional-only card, 66kV → 72kV clearance band lookup, Fittings & Structures rendering + collapsible reference, and the three 2026-07-28 additions: Pole Planting picker + PDF, Construction sequence/rules/interactive 35-item checklist + checklist PDF with unticked-item marking, Faults & Maintenance lightning calc + fault reference + glossary) | Engine proved by 455/455 automated tests (20 new this session) and all new UI strings grep-confirmed in the compiled bundle — but UI wiring, touch behaviour, checklist tick state, and PDF rendering still need the reference device | Next `assembleRelease` build+install pass; add scenarios to a `docs/on_device_checklist_overhead_reticulation.md` (not yet written — cheap to generate from the tests' expected values) | Should — no safety-critical go/no-go outputs in this module (unlike Arc Flash/IR-PI-DAR), but the pre-energization checklist is a compliance record and its PDF must render correctly before anyone hands it over |
| **DISSCAAO1 Rev 2 (pole/stay planting & backfilling procedure) not accessible** — Pole Planting reproduces only the §4.5.9 Table 6 depths; the procedural standard the clause references is honestly flagged in-app and in the PDF note rather than paraphrased from guesswork | No accessible copy found; fabricating a backfilling/compaction procedure would be worse than flagging | Revisit if an accessible copy of DISSCAAO1 surfaces; until then the in-app flag stands | Cosmetic — the table values are the field-useful part; the procedure flag is honest |
| **Pole Spacing still 22 kV-only** (C=0.4m is the only verified constant) — unchanged from 2026-07-27, restated so it isn't lost behind the new sub-tabs | The 11/33 kV C-constants were not found in accessible source text | A registered SANS 10280-1 copy, or an accessible source stating C for other voltages | Cosmetic-to-should — honestly gated in-app |
| **Stay-wire size/breaking-load tables still blocked** (BS 183 / ASTM A475) — guy-grip selection correctly redirects to stay-strand diameter but can't yet look the strand itself up | No verified accessible source found in two sessions of looking | A verified BS 183/ASTM A475 table, or a supplier datasheet chain solid enough to cite | Cosmetic — the redirect is honest; this is a nice-to-have lookup |
| **Sag & Tension / pole strength charter boundary — deferred a third time (2026-07-28), deliberately** | "Cover everything in one sweep" explicitly excluded it: it's a scoping decision (structural design territory: wind/ice zones, OHS Act §40% UTS interplay, foundation/soil inputs), not a build item | A dedicated three-question scoping session with Hertz before any code | Tracked — the repeated deferral is the system working, not debt accruing |
